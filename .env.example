# Shopify -- still needed short term, until the catalog migration to
# Supabase actually happens and src/lib/shopify/* call sites are removed.
SHOPIFY_STORE_DOMAIN=
SHOPIFY_STOREFRONT_ACCESS_TOKEN=

# Supabase (Label AshB project)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Razorpay -- test mode keys first, live keys only after the DNS cutover
# and a successful live test order.
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
NEXT_PUBLIC_RAZORPAY_KEY_ID=
